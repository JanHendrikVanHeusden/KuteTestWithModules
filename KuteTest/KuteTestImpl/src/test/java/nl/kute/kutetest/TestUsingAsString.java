package nl.kute.kutetest;

import org.junit.jupiter.api.Test;

class TestUsingAsString {

    // The actual test is that demonstrate that the project classes run without issues.
    // Any assertions, if needed, are done in the project classes.
    // So being able to execute the test is enough.

    @Test
    void testUsingAsString() {
        UsingAsString.main(null);
    }

    @Test
    void testUsingAsStringSubClass() {
        UsingAsString.main(null);
    }
}
